#summary converts a degree to its radian number

This method converts it's argument (degrees) into it's radian representation.

*Arguments:*

  * iDegrees - value in degrees.

*Example:*

{{{
var iRadians = jgen.Math.deg2rad(90);
}}}